import 'package:flutter/material.dart';

class EntityChatPage extends StatelessWidget {
  const EntityChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.greenAccent,
      child: const Center(child: Text("Incoming...")),
    );
  }
}
